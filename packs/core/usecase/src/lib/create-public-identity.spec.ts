import { CreatePublicIdentity } from './create-public-identity';

describe('CreatePublicIdentity', () => {
  it('should create an instance', () => {
    expect(new CreatePublicIdentity()).toBeTruthy();
  });
});
