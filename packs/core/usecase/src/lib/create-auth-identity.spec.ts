import { CreateAuthIdentity } from './create-auth-identity';

describe('CreateAuthIdentity', () => {
  it('should create an instance', () => {
    expect(new CreateAuthIdentity()).toBeTruthy();
  });
});
